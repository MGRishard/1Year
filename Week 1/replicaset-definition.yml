apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: myapp-replicaset
  labels:
    app: myapp
    type: front-end
    name: nginx
spec:
  replicas: 74
  selector:
    matchLabels:
      type: front-end
  template:
    metadata:
      labels:
        app: myapp
        type: front-end
        name: nginx
    spec:
      containers:
      - name: nginx-container
        image: nginx